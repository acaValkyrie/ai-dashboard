{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the dashboard window",
  "windows": ["main"],
  "permissions": ["core:default", "mcp-bridge:default"]
}
